Flask==2.3.3
opencv-python==4.8.1.78
ultralytics==8.0.196
numpy==1.24.3
Pillow==10.0.0
torch==2.0.1
torchvision==0.15.2
# Additional dependencies can be added when some system not recognizes the above packages. For example, if you encounter issues with torch or torchvision, you may need to specify the version compatible with your CUDA version.   